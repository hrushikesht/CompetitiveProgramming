#include <iostream>
#include <stdio.h>
#include <algorithm>
#include <stdbool.h>
#include <string>
#define pb push_back
#define N 750000

using namespace std;

bool myFunction(int x,int y){ return x<j; }


struct node
{
	int val;
	vector<*node> node_arr(NULL);
};

int main()
{
	int t=0;
	scanf("%d",&t);

	while(t--)
	{
		vector<int> arr;

		int n=0;
		scanf("%d",&n);

		for(int i=0;i<n;i++)
		{
			int temp;
			scanf("%d",&temp);
			arr.pb(temp);
		}

		sort(arr.begin(),arr.end(),myFunction);

		struct node *pointer;

		pointer=(struct node *)malloc(n*sizeof(node));
		(pointer[0])->val=arr.at(0);

		vector<int>::iterator it;
		int prev=arr.at(0);
		int pointer_index=1;

		for(it=(arr.begin()+1),it<arr.end();it++)
		{
			for(int i=0;i<pointer_index;i++)
			{
				while
			}
		}


	}
}